node_modules/
.wrangler/

# Local secrets — never commit. Keep only the .example template.
.dev.vars
.dev.vars.*
!.dev.vars.example
.env
.env.*
!.env.example

*.log

# Generated by scripts/seed_catalog.py — regenerate before applying, don't commit.
catalog-seed.sql
scripts/__pycache__/
