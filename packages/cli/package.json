{
  "name": "@graflethq/cli",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "graflet CLI — sign in with GitHub and download docs + knowledge graphs (ticket 03: login/logout).",
  "type": "module",
  "bin": { "graflet": "dist/index.js" },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@napi-rs/keyring": "^1.1.9"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "4.1.10"
  }
}
