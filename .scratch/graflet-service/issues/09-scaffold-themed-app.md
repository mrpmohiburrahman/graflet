# Scaffold the Vite + shadcn app shell with the decided theme tokens

Type: task
Status: open
Mode: AFK
Blocked by: —

## Question

Stand up the empty app shell - Vite + React + TS, Tailwind v4, shadcn/ui on Base UI - and paste the fixed tweakcn token block (teal accent, warm-stone neutrals, --radius 0.4rem) into index.css; verify light and dark both render intentionally. Unblocks building the real screens once the flow prototype lands.

---

**Why session-sized / why this type:** Mechanical scaffolding an agent runs alone; stack and tokens are already decided, so it needs no founder input and unblocks the eventual /implement.
